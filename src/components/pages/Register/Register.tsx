import Layout from 'components/layout/Layout';
import { FC } from 'react';

const Register: FC = () => {
  return (
    <Layout>
      <div>Register</div>
    </Layout>
  );
};

export default Register;
