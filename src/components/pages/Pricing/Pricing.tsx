import Layout from 'components/layout/Layout';
import { FC } from 'react';

const Pricing: FC = () => {
  return (
    <Layout>
      <div>Pricing</div>
    </Layout>
  );
};

export default Pricing;
