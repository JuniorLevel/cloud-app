import Layout from 'components/layout/Layout';
import { FC } from 'react';

const NotFound: FC = () => {
  return (
    <Layout>
      <div>NotFound</div>
    </Layout>
  );
};

export default NotFound;
